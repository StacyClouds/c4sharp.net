## 1. Regression tests

- [x] 1.1 Add failing renderer tests for visible shape-edge endpoints and centre interaction geometry.
- [x] 1.2 Add a failing editor asset test for mixed routed and direct connector updates.

## 2. Renderer routing geometry

- [x] 2.1 Render edge-clipped visible relationship paths and invisible centre-line interaction paths.
- [x] 2.2 Make connector endpoint rendering tests pass.

## 3. Live editor routing

- [x] 3.1 Update only relationship polylines and recompute visible endpoints during element drags.
- [x] 3.2 Run focused renderer and editor tests.

## 4. Live visible-path regression

- [x] 4.1 Add a failing editor asset test for clipping from the dragged centre.
- [x] 4.2 Recompute visible endpoints from live interaction centres and run focused tests.
